#include "../snake_game_app.h"
#include <catch2/catch.hpp>

TEST_CASE("Checking if tests work") {
    REQUIRE(1==1);
}

//Snake object not loading.
TEST_CASE("Check if snake moves correctly") {
    //Snake snake;
    //Snake snake(Rectf(glm::vec2(1.0f,1.0f), glm::vec2(10.0f,10.0f)));
    //snake.velocity = vec2(5.0,0);
    //snake.MoveSnake();
    //REQUIRE(snake.head.getX1() == 6.0f);
}

TEST_CASE("Check if snake grows after eating") {

}

TEST_CASE("Check collisions with walls") {
    //Snake snake;
}

TEST_CASE("Check collision with snake's body") {

}